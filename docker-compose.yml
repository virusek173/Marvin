services:
  marvin:
    env_file:
      - ./.env
    build:
      context: ./
      dockerfile: ./Dockerfile
    restart: unless-stopped
    volumes:
      - marvin_data:/usr/src/app/data

volumes:
  marvin_data:
